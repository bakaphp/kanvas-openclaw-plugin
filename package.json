{
  "name": "kanvas-openclaw-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw plugin scaffolding for Kanvas CRM, inventory, and orders operations.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "node --watch --loader ts-node/esm src/index.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
